---
title: Installation
description: Install Smithers for JSX-based workflow authoring.
---

Smithers' public authoring path is JSX. The full setup guide lives on the JSX installation page.

## Installation Guide

- [JSX Installation](/jsx/installation) — Install the package, configure TSX, and optionally add MDX prompts.

## Shared Requirements

Every Smithers workflow uses the same underlying runtime, so these show up everywhere:

- Bun
- local SQLite persistence
- optional model or provider credentials
- the shared runtime docs for observability, CLI usage, and integrations

## Next Steps

- [JSX Installation](/jsx/installation) — Set up TSX and optional MDX prompts.
- [CLI Reference](/cli/overview) — See how workflows are executed and resumed.
- [Tools Integration](/integrations/tools) — Learn about the built-in tool sandbox.
