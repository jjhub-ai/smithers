---
title: Quickstart
description: Start with the JSX quickstart for Smithers workflows.
---

Smithers' public quickstart is JSX-based and walks through the core workflow authoring loop: define schemas, create tasks, run the workflow, and inspect the result.

## Quickstart

- [JSX Quickstart](/jsx/quickstart) — Build a TSX workflow with `<Task>` components and `ctx.outputMaybe(...)`.

## Same Runtime, Different Authoring Style

The quickstart lands on the same durable runtime used everywhere else:

- steps are persisted to SQLite
- completed work resumes after restarts
- retries, approvals, and loops use the same engine
- observability and CLI tooling are shared

## Next Steps

- [JSX Quickstart](/jsx/quickstart) — Build a render-driven TSX workflow.
- [Hello World Example](/examples/hello-world) — See the smallest complete workflow.
- [Review Loop Guide](/guides/review-loop) — Move from a linear flow to iterative refinement.
